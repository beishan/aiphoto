# aiphoto 需求开发记录

本文件记录每次需求开发的目标、主要改动和验证结果，最新记录放在最前面。

## 记录规范

- 每次完成需求开发时新增一条记录，并与 `VERSION` 中的版本号保持一致。
- 记录日期、需求内容、主要改动、验证结果，以及必要的兼容性或部署说明。
- 纯调查、答疑或未产生项目变更的工作无需记录。

## 0.10.0 - 2026-09-12

### 需求

- 增加来源页追踪、跨任务链接去重、每日定时增量扫描、按链接查看采集图片，以及永久跳过图片管理。

### 主要改动

- 新增站点级已发现链接历史；旧任务链接自动回填，后续手工或定时任务只加入从未采集过的详情页。
- 网站配置支持启用每日扫描并选择执行时间；调度器避免同站点活动任务重叠，自动任务完成链接发现后等待人工确认下载。
- 新增图片—来源页关联表；同一图片出现在多个链接时保留全部来源，任务链接清单新增“查看图片”，可分页查看该链接下下载、失败或跳过的全部结果。
- 新增永久图片跳过清单；待整理区可批量将图片设为“以后跳过”，再次遇到相同图片 URL 时记录 `SKIPPED` 结果但不下载。
- 新增独立“跳过管理”页面，支持搜索、查看来源与命中次数，以及恢复采集。
- 将应用版本由 `0.9.0` 升级到 `0.10.0`。

### 验证

- 后端测试通过，覆盖跨任务链接去重、永久跳过规则和既有采集流程。
- 前端 TypeScript 类型检查与 Vite 生产构建通过。
- Docker Compose 配置解析通过，数据库增量 schema 已完成幂等迁移检查。

## 0.9.0 - 2026-09-12

### 需求

- 针对 t66y 图片列表页适配现有网络图片采集框架，并强化面向站点反爬规则的礼貌抓取能力。

### 主要改动

- 新增 t66y 图片版一键预设，自动填写列表入口、页面及图片域名白名单、帖子与分页选择器。
- 识别帖子正文 `#conttpc` 内 `ess-data` 原图地址，避开占位图、头像、签名和评论区图片。
- 新增站点级最小请求间隔，t66y 预设按其 `robots.txt` 设置为每个页面域名 10 秒一次，并继续叠加全局限速、临时错误重试和代理故障切换。
- 数据库 schema、后端规则快照和前端网站配置同步支持站点级请求间隔。
- 将应用版本由 `0.8.0` 升级到 `0.9.0`。

### 验证

- 使用目标站点公开 HTML 验证列表、下一页、正文与 `ess-data` 原图选择器。
- 增加 `ess-data` 优先提取测试；后端测试与前端生产构建通过。
- Docker Compose 配置解析通过。

## 0.8.0 - 2026-09-11

### 需求

- 重构全部前端页面的 UI，尽可能采用 Element Plus 组件，并统一适配 macOS 26 视觉主题。

### 主要改动

- 新增应用级视觉系统，统一页面宽度、留白、圆角、材质、阴影、边框、动效和响应式断点。
- 重构顶栏主题切换，并为相册、分类、标签、图库、收藏、时间线、人物与详情页统一 Element Plus 按钮、卡片、骨架屏、评分和空状态。
- 对搜索、设置、文件夹、人物管理及爬虫等复杂工作区统一面板层级、筛选工具栏和 macOS 26 Liquid Glass 材质。
- 登录页、更多页、Dock、照片预览和批量操作区同步纳入新的视觉语言，同时保留既有业务流程和路由行为。
- 支持减少透明度与减少动态效果偏好，并优化窄屏下的内容边距和悬浮交互。
- 将应用版本由 `0.7.0` 升级到 `0.8.0`。

### 验证

- 前端 TypeScript 类型检查与 Vite 生产构建通过。
- Docker Compose 配置解析通过。

## 0.7.0 - 2026-09-11

### 需求

- 大幅重构图片爬虫管理页面，优先使用 Element Plus 组件并适配 macOS 26 视觉主题。

### 主要改动

- 重建页面头部与工作区导航，使用 Element Plus 卡片、分段选择器、状态标签和按钮统一交互语言。
- 将采集概览重构为指标卡、四阶段步骤条、快速开始时间线和最近任务表格。
- 将网站与解析规则工作区重构为菜单侧栏、响应式表单、规则编辑器、测试结果折叠面板。
- 将任务工作区重构为队列侧栏、任务进度、描述统计、操作区和图片页数据表格。
- 将待整理区重构为筛选面板、吸附式批量工具栏、元数据编辑、导入历史表格和图片卡片网格。
- 新增爬虫页面独立响应式样式，适配 macOS 26 Liquid Glass 的半透明材质、边缘高光、层级阴影，并兼容减少透明度和减少动态效果设置。
- 保留既有采集、确认、下载、去重、标注和入库业务逻辑及 API 行为。
- 将应用版本由 `0.6.1` 升级到 `0.7.0`。

### 验证

- 前端 TypeScript 类型检查通过。
- Vite 生产构建通过。
- 本地浏览器完成暗色主题与 macOS 26 主题下的采集概览、网站工作区和新增网站弹窗视觉检查。
- Docker Compose 配置解析通过。

## 0.6.1 - 2026-09-11

### 需求

- 将爬虫页面的“新增网站”由页内表单改为弹窗交互。

### 主要改动

- 两个新增网站入口统一打开独立弹窗，集中填写网站入口、允许域名和任务安全上限。
- 页内表单只用于编辑已选网站；没有网站时显示引导空状态。
- 新建弹窗使用独立表单状态，取消操作不会清空当前网站或解析规则。
- 创建成功后自动关闭弹窗、选中新网站并载入其解析规则。
- 将应用版本由 `0.6.0` 升级到 `0.6.1`。

### 验证

- 前端 TypeScript 类型检查通过。
- Vite 生产构建通过。
- 本地浏览器完成新增网站弹窗的视觉与交互检查。
- Docker Compose 配置解析通过。

## 0.6.0 - 2026-09-11

### 需求

- 参照 `ai-book` 项目的爬虫逻辑和管理台页面，重构图片爬虫页面。

### 主要改动

- 将图片采集页面重组为“采集概览、网站与规则、采集任务、待整理区”四个工作区。
- 新增采集指标、四阶段流程说明、最近任务、任务状态和进度展示。
- 网站配置按身份入口、安全上限分区，解析规则按链接发现和原图提取分区。
- 保留并串联“发现链接 → 人工确认 → 下载校验 → 整理入库”的图片采集流程。
- 增强任务切换、失败重试、人工确认、整理任务选择和移动端布局。
- 将应用版本由 `0.5.2` 升级到 `0.6.0`。

### 验证

- 前端 TypeScript 类型检查通过。
- Vite 生产构建通过。
- 本地桌面尺寸下完成采集概览、网站与规则工作区的视觉检查。
- Docker Compose 生产配置解析通过。

## 0.5.2 - 2026-09-11

### 需求

- 增加统一的需求开发记录文件，用于持续记录每次开发内容。

### 主要改动

- 新增 `DEVELOPMENT_LOG.md`，定义开发记录的内容和维护规则。
- 在 `AGENTS.md` 中加入每次需求开发后同步维护本文件的要求。
- 将应用版本由 `0.5.1` 升级到 `0.5.2`。

### 验证

- 校验所有应用构建元数据与根目录版本号一致。
- 校验 Docker Compose 生产配置可以正常解析。

## 0.5.1 - 2026-09-11

### 需求

- 避免 AI 服务在日常发版时反复下载、安装体积较大的 Python 依赖。

### 主要改动

- 将 AI 镜像的版本元数据移动到 Python 依赖安装层之后，避免版本号变化导致依赖层缓存失效。
- 为 pip BuildKit 缓存设置稳定的共享缓存 ID `aiphoto-ai-pip`。
- 补充 Jenkins NAS 部署缓存说明。

### 验证

- Docker Compose 生产配置解析通过。
- Dockerfile 中依赖安装层位于应用版本参数之前。

### 部署说明

- 首次采用新缓存结构时仍需完整安装依赖；后续基础镜像和 `requirements.txt` 未变化时将复用依赖层。
